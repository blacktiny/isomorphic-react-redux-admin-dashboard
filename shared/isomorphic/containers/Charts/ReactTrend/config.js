const data = [0, 2, 5, 9, 5, 10, 3, 5, 0, 0, 1, 8, 2, 9, 0];
const radius = 1.5;
const strokeWidth = 0.5;
const strokeLinecap = 'round'; // butt, square
const gradient = ['purple', 'violet'];
const autoDrawEasing = 'ease-out'; // linear, ease, ease-in, cubic-bezier
const autoDraw = true;
const autoDrawDuration = 3000;
const smooth = true;

export {
  data,
  radius,
  strokeWidth,
  strokeLinecap,
  gradient,
  autoDraw,
  autoDrawDuration,
  autoDrawEasing,
  smooth,
};
